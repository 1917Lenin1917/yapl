fn bubble(arr: Array): Array {
  for let i = 0; i < arr.size(); i = i + 1; {
    for let j = i + 1; j < arr.size(); j = j + 1; {
			if arr[i] > arr[j] {
				const temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
    }
  }
	return arr;
}

print(bubble([5, 4, 3, 2, 1]));
