fn bubble(arr: Array): Array {
	let i=0;
	while i < arr.size() {
		let j=i+1;
		while j < arr.size() {
			if arr[i] > arr[j] {
				const temp = arr[i];
				arr[i] = arr[j];
				arr[j] = temp;
			}
			j = j + 1;
		}
		i = i + 1;
	}
	return arr;
}

print(bubble([5, 4, 3, 2, 1]));
